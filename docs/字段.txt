费用明细表(fee_list_d2409)
BKKP_SN（记账流水号）
MDTRT_ID（就诊 ID）
FEE_OCUR_TIME（费用发生时间）
SETL_ID（结算 ID）
RX_DRORD_NO（处方 / 医嘱号）
FIXMEDINS_CODE（定点医药机构编号）
PSN_NO（人员编号）
PSN_INSU_RLTS_ID（人员参保关系 ID）
INSU_ADMDVS（参保所属医保区划）
PAY_LOC（支付地点类别）
MED_TYPE（医疗类别）
CNT（数量）
PRIC（单价）
DET_ITEM_FEE_SUMAMT（明细项目费用总额）
PRIC_UPLMT_AMT（定价上限金额）
SELFPAY_PROP（自付比例）
FULAMT_OWNPAY_AMT（全自费金额）
OVERLMT_SELFPAY（超限价自费费用）
PRESELFPAY_AMT（先行自付金额）
INSCP_AMT（符合范围金额）
CVLSERV_BEDFEE_AMT（公务员床位费金额）
MEDINS_DISC_AMT（医院减免金额）
CHRGITM_LV（收费项目等级）
HILIST_CODE（医保目录编码）
HILIST_NAME（医保目录名称）
LIST_TYPE（目录类别）
MED_LIST_CODG（医疗目录编码）
MEDINS_LIST_CODG（医药机构目录编码）
MEDINS_LIST_NAME（医药机构目录名称）
MED_CHRGITM_TYPE（医疗收费项目类别）
PRODNAME（商品名）
SPEC（规格）
DOSFORM_NAME（剂型名称）
BILG_DEPT_CODG（开单科室编码）
BILG_DEPT_NAME（开单科室名称）
BILG_DR_CODE（开单医师代码）
ACORD_DEPT_CODG（受单科室编码）
ACORD_DEPT_NAME（受单科室名称）
ACORD_DR_CODE（受单医师代码）
LMT_USED_FLAG（限制使用标志）
HOSP_PREP_FLAG（医院制剂标志）
HOSP_APPR_FLAG（医院审批标志）
TCMDRUG_USED_WAY（中药使用方式）
PRODPLAC_TYPE（生产地类别）
BAS_MEDN_FLAG（基本药物标志）
HI_NEGO_DRUG_FLAG（医保谈判药品标志）
PRCU_DRUG_FLAG（贵重药品标志）
CHLD_MEDC_FLAG（儿童用药标志）
ETIP_FLAG（外检标志）
ETIP_HOSP_CODE（外检医院编码）
DSCG_TKDRUG_FLAG（出院带药标志）
LIST_SP_ITEM_FLAG（目录特项标志）
MATN_FEE_FLAG（生育费用标志）
DRT_REIM_FLAG（直报标志）
REIM_PROP（报销比例）
OPRN_OPRT_CODE（手术操作代码）
SIN_DOS_DSCR（单次剂量描述）
USED_FRQU_DSCR（使用频次描述）
PRD_DAYS（周期天数）
MEDC_WAY_DSCR（用药途径描述）
MEMO（备注）
VALI_FLAG（有效标志）
RID（数据唯一记录号）
UPDT_TIME（数据更新时间）
CRTER_ID（创建人 ID）
CRTE_TIME（数据创建时间）
OPTER_ID（经办人 ID）
OPT_TIME（经办时间）
POOLAREA_NO（统筹区编号）
RX_CIRC_FLAG（处方流转标志）
DISE_CODG（病种编码）
CHRG_BCHNO（收费批次号）
INIT_FEEDETL_SN（原费用明细流水号）

就诊信息表(mdtrt_d2409)
MDTRT_ID（就诊 ID）
MEDINS_SETL_ID（医药机构结算 ID）
PSN_NO（人员编号）
PSN_INSU_RLTS_ID（人员参保关系 ID）
PSN_CERT_TYPE（人员证件类型）
GEND（性别）
NATY（民族）
BRDY（出生日期）
AGE（年龄）
INSUTYPE（险种类型）
PSN_TYPE（人员类别）
CLCT_GRDE（缴费档次）
FLXEMPE_FLAG（灵活就业标志）
NWB_FLAG（新生儿标志）
INSU_ADMDVS（参保所属医保区划）
EMP_NO（单位编号）
EMP_TYPE（单位类型）
ECON_TYPE（经济类型）
AFIL_INDU（所属行业）
AFIL_RLTS（隶属关系）
EMP_MGT_TYPE（单位管理类型）
PAY_LOC（支付地点类别）
FIXMEDINS_CODE（定点医药机构编号）
HOSP_LV（医院等级）
FIX_BLNG_ADMDVS（定点归属医保区划）
LMTPRIC_HOSP_LV（限价医院等级）
DEDC_HOSP_LV（起付线医院等级）
BEGNTIME（开始时间）
ENDTIME（结束时间）
MDTRT_CERT_TYPE（就诊凭证类型）
MED_TYPE（医疗类别）
RLOC_TYPE（异地安置类别）
ARS_YEAR_IPT_FLAG（跨年度住院标志）
PRE_PAY_FLAG（先行支付标志）
YEAR（年度）
REFL_OLD_MDTRT_ID（转诊前就诊 ID）
IPT_OTP_NO（住院 / 门诊号）
MEDRCDNO（病历号）
CHFPDR_CODE（主诊医师代码）
ADM_DIAG_DSCR（入院诊断描述）
ADM_DEPT_CODG（入院科室编码）
ADM_DEPT_NAME（入院科室名称）
ADM_BED（入院床位）
WARDAREA_BED（病区床位）
TRAF_DEPT_FLAG（转科室标志）
DSCG_MAINDIAG_CODE（住院主诊断代码）
DSCG_DEPT_CODG（出院科室编码）
DSCG_DEPT_NAME（出院科室名称）
DSCG_BED（出院床位）
DSCG_WAY（离院方式）
MAIN_COND_DSCR（主要病情描述）
DISE_NO（病种编号）
DISE_NAME（病种名称）
OPRN_OPRT_CODE（手术操作代码）
OPRN_OPRT_NAME（手术操作名称）
OTP_DIAG_INFO（门诊诊断信息）
INHOSP_STAS（在院状态）
DIE_DATE（死亡日期）
IPT_DAYS（住院天数）
GESO_VAL（孕周数）
BIRCTRL_TYPE（计划生育手术类别）
FETTS（胎次）
FETUS_CNT（胎儿数）
MATN_TYPE（生育类别）
PREY_TIME（妊娠时间）
LATECHB_FLAG（晚育标志）
PRET_FLAG（早产标志）
FPSC_NO（计划生育服务证号）
BIRCTRL_MATN_DATE（计划生育手术或生育日期）
COP_FLAG（伴有并发症标志）
TRT_DCLA_DETL_SN（待遇申报明细流水号）
VALI_FLAG（有效标志）
MEMO（备注）
RID（数据唯一记录号）
UPDT_TIME（数据更新时间）
CRTER_ID（创建人 ID）
CRTE_TIME（数据创建时间）
OPTER_ID（经办人 ID）
OPT_TIME（经办时间）
POOLAREA_NO（统筹区编号）
DSCG_MAINDIAG_NAME（住院主诊断名称）
ADM_CATY（入院科别）
DSCG_CATY（出院科别）
TTP_PAY_FLAG（第三方赔付标志）
TTP_PAY_PROP（第三方赔付比例）

结算信息表(setl_d2409)
SETL_ID（结算 ID）
MDTRT_ID（就诊 ID）
INIT_SETL_ID（原结算 ID）
PSN_NO（人员编号）
PSN_INSU_RLTS_ID（人员参保关系 ID）
PSN_CERT_TYPE（人员证件类型）
GEND（性别）
NATY（民族）
BRDY（出生日期）
AGE（年龄）
INSUTYPE（险种）
PSN_TYPE（人员类别）
CLCT_GRDE（缴费档次）
FLXEMPE_FLAG（灵活就业标志）
NWB_FLAG（新生儿标志）
INSU_ADMDVS（参保所属医保区划）
EMP_NO（单位编号）
EMP_TYPE（单位类型）
ECON_TYPE（经济类型）
AFIL_INDU（所属行业）
AFIL_RLTS（隶属关系）
EMP_MGT_TYPE（单位管理类型）
PAY_LOC（支付地点类别）
FIXMEDINS_CODE（定点医药机构编号）
HOSP_LV（医院等级）
FIX_BLNG_ADMDVS（定点归属医保区划）
LMTPRIC_HOSP_LV（限价医院等级）
DEDC_HOSP_LV（起付线医院等级）
BEGNDATE（开始日期）
ENDDATE（结束日期）
SETL_TIME（结算时间）
MDTRT_CERT_TYPE（就诊凭证类型）
MED_TYPE（医疗类别）
SETL_TYPE（结算类别）
CLR_TYPE（清算类别）
CLR_WAY（清算方式）
PSN_SETLWAY（个人结算方式）
MEDFEE_SUMAMT（医疗费总额）
FULAMT_OWNPAY_AMT（全自费金额）
OVERLMT_SELFPAY（超限价自费费用）
PRESELFPAY_AMT（先行自付金额）
INSCP_AMT（符合范围金额）
DECD_STD（起付标准）
CRT_DEDC（本次起付线）
ACT_PAY_DEDC（实际支付起付线）
HIFP_PAY（统筹基金支出）
POOL_PROP_SELFPAY（基本医疗统筹支付比例）
HI_AGRE_SUMFEE（医保认可费用总额）
CVLSERV_PAY（公务员医疗补助基金支出）
HIFES_PAY（补充医疗保险基金支出）
HIFMI_PAY（大病补充医疗保险基金支出）
HIFOB_PAY（大额医疗补助基金支出）
HIFDM_PAY（伤残人员医疗保障基金支出）
MAF_PAY（医疗救助基金支出）
OTHFUND_PAY（其它基金支付）
FUND_PAY_SUMAMT（基金支付总额）
PSN_PAY（个人支付金额）
ACCT_PAY（个人账户支出）
CASH_PAYAMT（现金支付金额）
OWNPAY_HOSP_PART（自费中医院负担部分）
REFD_SETL_FLAG（退费结算标志）
YEAR（年度）
DISE_NO（病种编号）
DISE_NAME（病种名称）
INVONO（发票号）
MANL_REIM_REA（零星报销原因）
MEMO（备注）
POOLAREA_NO（统筹区编号）
VALI_FLAG（有效标志）
CRTER_ID（创建人 ID）
CRTE_TIME（数据创建时间）
OPTER_ID（经办人 ID）
OPT_TIME（经办时间）
UPDT_TIME（数据更新时间）
MEDINS_SETL_ID（医药机构结算 ID）
BALC（余额）
ACCT_MULAID_PAY（账户共济支付金额）
CAL_IPT_CNT（计算住院次数标志）
SETL_CASHPAY_WAY（结算现金支付方式）
MID_SETL_FLAG（中途结算标志）
ACCT_USED_FLAG（账户使用标志）

结算诊断信息表(setl_diag_list_d2409)
DIAG_INFO_ID（诊断信息 ID）
MDTRT_ID（就诊 ID）
PSN_NO（人员编号）
INOUT_DIAG_TYPE（出入院诊断类别）
DIAG_TYPE（诊断类别）
MAINDIAG_FLAG（主诊断标志）
DIAG_SRT_NO（诊断排序号）
DIAG_CODE（诊断代码）
DIAG_NAME（诊断名称）
ADM_COND（入院病情）
DIAG_DEPT（诊断科室）
DIAG_DR_CODE（诊断医师代码）
DIAG_TIME（诊断时间）
VALI_FLAG（有效标志）
RID（数据唯一记录号）
UPDT_TIME（数据更新时间）
CRTER_ID（创建人 ID）
CRTE_TIME（数据创建时间）
OPTER_ID（经办人 ID）
OPT_TIME（经办时间）
OPTINS_NO（经办机构编号）
EVT_INST_ID（事件实例 ID）