server:
  port: 8080  # 需要针对每个服务调�?

spring:
  application:
    name: rule-engine-service
  datasource:
    url: jdbc:mysql://localhost:3306/medical_db  # 与data-service使用相同数据库
    username: root
    password: root
