# 使用Python 3.11-alpine作为基础镜像（更轻量）
FROM python:3.11-alpine

# 设置维护者信息
LABEL maintainer="DeepRisk Team"

# 设置工作目录
WORKDIR /app

# 安装系统依赖（Alpine版本）
RUN apk add --no-cache gcc g++ musl-dev gfortran openblas-dev

# 复制requirements文件
COPY requirements.txt .

# 安装Python依赖
RUN pip install --no-cache-dir -r requirements.txt

# 安装项目依赖
RUN pip install --no-cache-dir nacos-sdk-python

# 复制项目文件
COPY . .

# 暴露端口
EXPOSE 8000

# 运行应用
CMD ["python", "run.py"]